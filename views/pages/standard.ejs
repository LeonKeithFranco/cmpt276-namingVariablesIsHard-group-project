<!DOCTYPE html>
<html lang="en">

<head>
    <% include ../partials/header.ejs %>
    <script type="text/javascript" src="/scripts/game-engine.js" defer></script>
    <script type="text/javascript" src="/scripts/standard.js" defer></script>
    <script>
        function playAudio() {
            let x = document.getElementById("wrongSound");
            x.play();
        }
    </script>
    <link rel="stylesheet" type="text/css" href="/stylesheets/standard.css">
</head>

<body>
    <audio id="rightSound">
        <source src="/assets/audio/c3.mp3" type="audio/mpeg">
        Your browser does not support the audio element.
    </audio>
    <audio id="wrongSound">
        <source src="/assets/audio/wrong.mp3" type="audio/mpeg">
        Your browser does not support the audio element.
    </audio>
    <button onclick="playAudio()" type="button">Play Audio</button>
    <center>
        <img id="headerimage" src="/assets/scribbltaireio.png">
        <h1 class="text">Standard Mode</h1>
        <% if (difficulty === 'easy') { %>
            <% include ../partials/4-drawing.ejs %>
        <% } else if (difficulty === 'normal') { %>
            <% include ../partials/2-drawing.ejs %>
        <% } else if (difficulty === 'hard') { %>
            <% include ../partials/1-drawing.ejs %>
        <% } else { %>
            <h1><strong>Something has gone horribly wrong</strong></h1>
        <% } %>
        <div id="inputSection">
            <p id="score" class="text"></p>
            <div id="hintDisplay">
                <p id="hintTitle" class="text">Hint: </p>
                <p id="hint" class="text"></p>
            </div>
            <br>
            <input id="wordInput" type="text" placeholder="Word" autofocus>
            <button id="submitGuessButton" class="button">Guess</button><br>
            <button id="playAgainButton" class="button">Play Again</button>
        </div>
        <p id="standardModeRules" class="text">
            Rules:<br>
            Guess the category of these drawings.<br>
            You have infinite time, but only one guess.<br>
            If you guess correctly, then you'll get a new set of drawings.<br>
            Your score increases with each set of drawings you guess correctly.<br>
            If you guess incorrectly, then the game ends, so guess well!
        </p>
        <button id="backToMainMenuButton" class="button">Go Back to Main Menu</button>
    </center>
    
</body>

</html>
