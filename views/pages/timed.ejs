<!DOCTYPE html>
<html lang="en">

<head>
    <% include ../partials/header.ejs %>
    <script>
        let difficulty = "<%= difficulty %>";
    </script>
    <script type="text/javascript" src="/scripts/game-engine.js" defer></script>
    <% include ../partials/flash.ejs %>
    <script type="text/javascript" src="/scripts/timed.js" defer></script>
    <link rel="stylesheet" type="text/css" href="/stylesheets/timed.css">
</head>

<body>
    <center>
        <img id="headerimage" src="/assets/scribbltaireio.png">
        <h1 class="text">Timed Mode</h1>
        <% if (difficulty === 'easy') { %>
        <% include ../partials/4-drawing.ejs %>
        <% } else if (difficulty === 'normal') { %>
        <% include ../partials/2-drawing.ejs %>
        <% } else if (difficulty === 'hard') { %>
        <% include ../partials/1-drawing.ejs %>
        <% } else { %>
        <h1><strong>Something has gone horribly wrong</strong></h1>
        <% } %>
        <div id="inputSection">
            <p id="score" class="text"></p>
            <p id="time" class="text">0</p>
            <div id="hintDisplay">
                <p id="hintTitle" class="text">Hint: </p>
                <p id="hint" class="text"></p>
            </div>
            <br>
            <input id="wordInput" type="text" placeholder="Word" autofocus>
            <button id="submitGuessButton" class="button">Guess</button><br>
            <button id="playAgainButton" class="button">Play Again</button>
        </div>
        <p id="timedModeRules" class="text">
            Rules:<br>
            Guess as many drawing sets as you can in the time limit.<br>
            Guessing correctly will bring you a new drawing set.<br>
            Incorrect guesses will not count towards your score.<br>
            With each incorrect guess, a letter in the answer will be revealed for you. <br>
            You may make as many guesses as you like, but you have limited time so guess quickly!
        </p>
        <button id="backToMainMenuButton" class="button">Go Back to Main Menu</button>
    </center>

</body>

</html>